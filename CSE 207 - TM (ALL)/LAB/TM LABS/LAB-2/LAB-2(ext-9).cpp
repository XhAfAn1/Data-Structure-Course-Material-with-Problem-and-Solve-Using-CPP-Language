void removefirst(Node *node, int n)
{
    if (head == NULL)
    {
        return false;
    }
    if(head->vale == n)
    {
        return true;
    }
    else

    {
        void result = findNum (head->next, n)
        if(result == true)
        {
            Node *node = node;

            free(node)

        }
        else
        {
            return false;
        }
    }

}

